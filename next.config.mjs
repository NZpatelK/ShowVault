/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/ShowVault",
    // output: "export", 
    images: {
      unoptimized: true,
    },
    reactStrictMode: true,
  };

export default nextConfig;
